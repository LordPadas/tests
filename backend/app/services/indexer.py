def index_directory(path: str) -> None:
    # Placeholder for indexing logic; in MVP this runs in a separate worker
    # and writes metadata to registry. No actual IO here.
    print(f"[Indexer] would index: {path}")
