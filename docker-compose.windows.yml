version: "3.9"
services:
  db:
    volumes:
      - "C:/data/db:/var/lib/postgresql/data"
  backend:
    environment:
      - DB_URL=postgresql://postgres:postgres@db:5432/app
  ollama:
    volumes: []
